# 🚀 كيفية تشغيل المشروع

## ✅ الطريقة الموصى بها: استخدام Herd

إذا كنت تستخدم **Laravel Herd** (الطريقة الأسهل):

### 1. تأكد من أن Herd يعمل:

- افتح تطبيق Herd
- تأكد من أن المشروع `backend` موجود في قائمة المشاريع
- Herd سيشغّل المشروع تلقائياً على `http://backend.test` "مهم"

### 2. تشغيل Frontend (React):

افتح terminal:

```bash
cd frontend
npm run dev
```

**الواجهة ستعمل على:** `http://localhost:5173`

**⚠️ ملاحظة:** مع Herd، **لا تحتاج** لتشغيل `php artisan serve` يدوياً!

---

## الطريقة البديلة: استخدام Laravel Serve العادي

### 1. تأكد من إعدادات API:

افتح `frontend/api.ts` وغير السطر الأول:

```typescript
const BASE_URL = "http://localhost:8000/api";
```

### 2. تشغيل Backend:

```bash
cd backend
php artisan migrate
php artisan db:seed
php artisan serve
```

**الخادم سيعمل على:** `http://localhost:8000`

### 3. تشغيل Frontend:

افتح terminal جديد:

```bash
cd frontend
npm run dev
```

**الواجهة ستعمل على:** `http://localhost:5173`

---

## 📝 بيانات تسجيل الدخول:

### مستخدم عادي:

- **Username:** `jimy`
- **Password:** `jimy`

### مسؤول (Admin):

- **Username:** `admin`
- **Password:** `admin`

---

## ⚠️ ملاحظات مهمة:

1. **تأكد من تشغيل Backend قبل Frontend**
2. **إذا ظهرت رسالة "لا يمكن الاتصال بالخادم":**

   - تأكد من أن Laravel يعمل
   - تحقق من المنفذ في `api.ts`
   - تأكد من أن CORS مفعل

3. **لإعادة تعيين قاعدة البيانات:**

   ```bash
   cd backend
   php artisan migrate:fresh --seed
   ```

4. **إذا كنت تستخدم XAMPP:**
   - تأكد من أن Apache و MySQL يعملان
   - تأكد من إعداد virtual host `backend.test`

---

## 🎯 الخطوات السريعة (مع Herd):

```bash
# 1. تأكد من أن Herd يعمل (افتح تطبيق Herd)
# 2. شغّل Frontend فقط:
cd frontend
npm run dev
```

ثم افتح المتصفح على: `http://localhost:5173`

**Backend متاح تلقائياً على:** `http://backend.test/api`

---

## 🔧 حل مشكلة المنفذ المشغول:

إذا ظهرت رسالة "Failed to listen on port 8080":

- **مع Herd:** لا تحتاج لتشغيل `php artisan serve` - Herd يشغله تلقائياً
- **بدون Herd:** استخدم منفذ آخر:
  ```bash
  php artisan serve --port=8000
  ```
  ثم غيّر `BASE_URL` في `frontend/api.ts` إلى:
  ```typescript
  const BASE_URL = "http://localhost:8000/api";
  ```
